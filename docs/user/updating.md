# Updating

This fork does not check for, download, or install T3 Code updates. Install a new build manually after
reviewing the changes. Close the app before replacing it, then reopen it with the same local data
location to keep your projects and conversations.

Provider version checks and provider maintenance remain available in Settings. These operate on the
coding providers you configured and are separate from updating the desktop app.
