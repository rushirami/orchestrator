# Local backend lifecycle

The desktop app starts and stops its local backend. Keep the desktop app running while working with
projects, terminals, and coding providers. On Windows, an enabled WSL backend is managed by the same
app.

This fork does not install a separate background service or download replacement server runtimes.
Service install, update, and uninstall commands are no longer part of the application.
